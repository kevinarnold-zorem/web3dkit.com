import{r as n,j as l}from"./index-ChUl42DD.js";const f=`<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<title>Web3DKit — Three.js components</title>
<meta name="description" content="A catalog of live three.js components with variants, controls and source on every page.">
<style>
  @font-face{
    font-family:'JB Mono'; font-style:normal; font-weight:400; font-display:block;
    src:url(data:font/woff2;base64,) format('woff2');
  }
  @font-face{
    font-family:'JB Mono'; font-style:normal; font-weight:600; font-display:block;
    src:url(data:font/woff2;base64,) format('woff2');
  }
  :root{
    --bg:#180a30;
    --ink:#f3ecff;
    --dim:#b9a6dd;
    --hot:#cbb6f5;
    --pad:clamp(20px,3.2vw,52px);
  }
  *{margin:0;padding:0;box-sizing:border-box}
  html,body{height:100%;overflow:hidden;background:var(--bg)}
  body{
    font-family:'JB Mono','JetBrains Mono',ui-monospace,'SF Mono',Menlo,monospace;
    -webkit-font-smoothing:antialiased;
    color:var(--ink);
    cursor:pointer;
    touch-action:none;
    user-select:none;-webkit-user-select:none;
    -webkit-tap-highlight-color:transparent;
  }

  /* ---- the field ---- */
  #stage{
    position:fixed;inset:0;background:var(--bg);z-index:0;
    -webkit-mask-image:linear-gradient(to bottom,transparent 0,#000 6%,#000 74%,transparent 100%);
            mask-image:linear-gradient(to bottom,transparent 0,#000 6%,#000 74%,transparent 100%);
  }
  canvas{position:absolute;inset:0;width:100%;height:100%;display:block}

  /* ---- progressive blur: five steps a side, each boxed to the strip it
         actually covers so the wide radii are not run over the whole band ---- */
  .gblur{position:fixed;left:0;right:0;z-index:2;pointer-events:none}
  .gblur>div{position:absolute;left:0;right:0}
  .gblur.top{top:0;height:20%}
  .gblur.bot{bottom:0;height:52%}

  .gblur.top>div:nth-child(1){top:40%;bottom:0;backdrop-filter:blur(1px);-webkit-backdrop-filter:blur(1px);
    mask:linear-gradient(to top,transparent 0%,#000 34%,#000 67%,transparent 100%)}
  .gblur.top>div:nth-child(2){top:20%;bottom:20%;backdrop-filter:blur(3px);-webkit-backdrop-filter:blur(3px);
    mask:linear-gradient(to top,transparent 0%,#000 34%,#000 67%,transparent 100%)}
  .gblur.top>div:nth-child(3){top:0;bottom:40%;backdrop-filter:blur(7px);-webkit-backdrop-filter:blur(7px);
    mask:linear-gradient(to top,transparent 0%,#000 34%,#000 100%)}
  .gblur.top>div:nth-child(4){top:0;bottom:60%;backdrop-filter:blur(15px);-webkit-backdrop-filter:blur(15px);
    mask:linear-gradient(to top,transparent 0%,#000 50%,#000 100%)}
  .gblur.top>div:nth-child(5){top:0;bottom:80%;backdrop-filter:blur(28px);-webkit-backdrop-filter:blur(28px);
    mask:linear-gradient(to top,transparent 0%,#000 100%)}

  .gblur.bot>div:nth-child(1){bottom:40%;top:0;backdrop-filter:blur(1px);-webkit-backdrop-filter:blur(1px);
    mask:linear-gradient(to bottom,transparent 0%,#000 34%,#000 67%,transparent 100%)}
  .gblur.bot>div:nth-child(2){bottom:20%;top:20%;backdrop-filter:blur(3px);-webkit-backdrop-filter:blur(3px);
    mask:linear-gradient(to bottom,transparent 0%,#000 34%,#000 67%,transparent 100%)}
  .gblur.bot>div:nth-child(3){bottom:0;top:40%;backdrop-filter:blur(7px);-webkit-backdrop-filter:blur(7px);
    mask:linear-gradient(to bottom,transparent 0%,#000 34%,#000 100%)}
  .gblur.bot>div:nth-child(4){bottom:0;top:60%;backdrop-filter:blur(15px);-webkit-backdrop-filter:blur(15px);
    mask:linear-gradient(to bottom,transparent 0%,#000 50%,#000 100%)}
  .gblur.bot>div:nth-child(5){bottom:0;top:80%;backdrop-filter:blur(28px);-webkit-backdrop-filter:blur(28px);
    mask:linear-gradient(to bottom,transparent 0%,#000 100%)}

  /* ---- scrims so type always clears the field ---- */
  .scrim{position:fixed;left:0;right:0;z-index:3;pointer-events:none}
  .scrim.top{top:0;height:22%;background:linear-gradient(to bottom,rgba(14,5,30,.72),rgba(14,5,30,0))}
  .scrim.bot{bottom:0;height:60%;background:linear-gradient(to top,rgba(14,5,30,.96) 0%,rgba(14,5,30,.88) 26%,rgba(14,5,30,.42) 58%,rgba(14,5,30,0) 100%)}

  /* ---- chrome ---- */
  .ui{position:fixed;z-index:4;pointer-events:none}
  .ui a,.ui button{pointer-events:auto}

  header{top:0;left:0;right:0;display:flex;align-items:center;justify-content:space-between;
    gap:24px;padding:clamp(18px,2.3vw,32px) var(--pad)}
  .logo{display:flex;align-items:center;gap:9px;color:var(--ink);text-decoration:none}
  .logo svg{width:21px;height:21px;flex:none;opacity:.92}
  .logo b{font-weight:600;font-size:15px;letter-spacing:.005em}
  nav{display:flex;align-items:center;gap:clamp(16px,2vw,30px)}
  nav a{color:var(--dim);text-decoration:none;font-size:11px;letter-spacing:.14em;
    text-transform:uppercase;transition:color .18s ease}
  nav a:hover,nav a:focus-visible{color:var(--ink)}

  .hero{left:0;right:0;bottom:0;padding:0 var(--pad) clamp(26px,3.6vw,52px)}
  .eyebrow{font-size:11px;letter-spacing:.16em;text-transform:uppercase;color:var(--hot);
    margin-bottom:clamp(12px,1.4vw,18px)}
  h1{font-weight:600;font-size:clamp(28px,4.4vw,57px);line-height:1.06;letter-spacing:-.025em;
    max-width:20ch;text-wrap:balance}
  .foot{display:flex;flex-wrap:wrap;align-items:flex-end;justify-content:space-between;
    gap:clamp(18px,2.4vw,40px);margin-top:clamp(18px,2.2vw,30px)}
  .foot p{color:var(--dim);font-size:clamp(12px,1.05vw,14px);line-height:1.65;max-width:60ch;flex:1 1 34ch}
  .cta{display:flex;align-items:center;gap:12px;flex:none}
  .btn{font:inherit;font-size:12.5px;font-weight:600;letter-spacing:.02em;border:1px solid transparent;
    padding:.78em 1.25em;background:var(--ink);color:#1a0a35;text-decoration:none;white-space:nowrap;
    transition:background .18s ease,color .18s ease,border-color .18s ease}
  .btn:hover,.btn:focus-visible{background:#fff}
  .btn.ghost{background:transparent;color:var(--hot);border-color:rgba(203,182,245,.3)}
  .btn.ghost:hover,.btn.ghost:focus-visible{color:var(--ink);border-color:rgba(203,182,245,.7)}
  :focus-visible{outline:1px solid var(--hot);outline-offset:3px}

  #tag{
    position:fixed;left:0;top:0;z-index:6;
    padding:.3em .49em;
    background:#f3ecff;color:#1b0a35;
    font-size:11px;line-height:1;letter-spacing:.1em;
    white-space:nowrap;
    pointer-events:none;
    opacity:0;
    will-change:transform,opacity;
    transform:translate3d(-200px,-200px,0);
  }
  #tag.on{opacity:1}
  #nope{position:fixed;inset:0;display:none;place-items:center;z-index:9;
    color:#9a83c4;font-size:12px;letter-spacing:.08em;text-align:center;padding:24px}

  @media (max-width:620px){
    .gblur.bot{height:56%}
    .scrim.bot{height:62%}
    nav a:nth-child(1){display:none}
    .foot{align-items:flex-start}
    .cta{width:100%}
    .btn{flex:1;text-align:center}
  }
</style>
</head>
<body>
<div id="stage"><canvas id="c"></canvas></div>

<div class="gblur top"><div></div><div></div><div></div><div></div><div></div></div>
<div class="gblur bot"><div></div><div></div><div></div><div></div><div></div></div>
<div class="scrim top"></div>
<div class="scrim bot"></div>

<header class="ui">
  <a class="logo" href="https://web3dkit.com">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.35"
         stroke-linejoin="round" stroke-linecap="round" aria-hidden="true">
      <path d="M12 2.6 21 7.8v10.4L12 23.4 3 18.2V7.8z"/>
      <path d="M12 13 3 7.8M12 13l9-5.2M12 13v10.4"/>
    </svg>
    <b>Web3DKit</b>
  </a>
  <nav>
    <a href="https://web3dkit.com">Components</a>
    <a href="https://web3dkit.com/pricing">Pricing</a>
    <a href="https://github.com/MengTo/web3dkit">GitHub</a>
  </nav>
</header>

<section class="ui hero">
  <p class="eyebrow">Three.js component library</p>
  <h1>Three.js components, ready to drop in.</h1>
  <div class="foot">
    <p>Every component ships with live renderers, a variant picker, controls and its own source
       — browse it, tune it, copy it out. Fifty are free and open source; Pro opens the rest of
       the catalog.</p>
    <div class="cta">
      <a class="btn" href="https://web3dkit.com">Browse components</a>
      <a class="btn ghost" href="https://github.com/MengTo/web3dkit">View source</a>
    </div>
  </div>
</section>

<div id="tag">CLICK &amp; HOLD</div>
<div id="nope">THIS PIECE NEEDS WEBGL2</div>
<script>
/* ------------------------------------------------------------------
   WEB3DKIT — RINGS
   A phrase coiled across concentric rings. The cursor melts glyphs
   into dots, the dots wire themselves together into a chaotic mesh,
   and holding then releasing fires a ripple that spins every ring
   it passes through. Two instanced draws: the mesh, then the type.
------------------------------------------------------------------ */
(() => {
  'use strict';

  const PHRASE   = 'WEB3DKIT 3D COMPONENTS THREE.JS ';
  const ALPHABET = ' ABCDEFGHIJKLMNOPQRSTUVWXYZ&0123456789.-';

  // --- geometry, measured off the original -----------------------------
  const REF      = 1728;              // reference viewport the numbers came from
  const K_DR     = 41.338 / REF;      // ring pitch, as a fraction of the design unit
  const K_R0     = 12.436 / REF;      // radius of ring 0
  const K_ADV_A  = 13.091 / REF;      // advance = S*K_ADV_A + R*K_ADV_B
  const K_ADV_B  = 0.01238;
  const EM_ADV   = 0.6518;            // mono advance + tracking, in em
  const CAP      = 0.730;             // JetBrains Mono cap height, in em

  const WAVE_V   = 1025 / REF;        // ripple speed, design units per second
  const WAVE_W   =  125 / REF;        // ripple melt band half-width
  const WAVE_AMP = 1.38;              // > 1 so the band melts a ring outright
  const CUR_IN   =  110 / REF;        // cursor: everything inside this is dots
  const CUR_OUT  =  320 / REF;        // cursor: untouched beyond this
  const SPIN_KICK= 0.60;              // rad/s handed to a ring as the wave lands
  const SPIN_TAU = 0.42;              // spin damping time constant
  const CHARGE_T = 1.0;               // seconds to a full charge
  const CHARGE_M = 0.07;              // how far charging drags the melt field up
  const INTRO_T  = 0.26;              // when the opening wave leaves the centre

  // the melt field: smooth noise in (angle, radius), sampled per glyph
  const N_CELLS  = 5;                 // noise cells around the circle
  const N_RADIAL = 0.05;              // noise cell depth, in design units
  const N_AMP    = 11.0;              // contrast — high enough to read as words
  const N_MIX    = 0.32;              // weight of the second octave
  const N_DRIFT  = 0.07;              // how fast the field evolves
  const MELT_W   = 0.015;             // glyph -> dot crossover width
  const R_BASE   = 0.028;             // radial bias of the field at the centre
  const R_SLOPE  = 0.060;             // ...and how it eases off outwards

  // the mesh the dots weave between themselves: each node owns three edges —
  // one along its ring and two outward — which triangulates the whole lattice
  const EDGES    = 3;
  const DOT_R    = 0.028;             // dot radius, as a fraction of font size
  const LINK_P   = 0.50;              // share of edges that survive
  const LINK_W   = 0.55;              // line half-width at 2x, device px
  const COARSE   = 0.58;              // above this the field spans wider polygons
  const JIT_R    = 0.22;              // node scatter across the ring, in pitches
  const JIT_T    = 0.45;              // ...and along it, in slot advances

  // the pointer pulls the mesh up out of the dark
  const GLOW_R   = 0.34;              // glow radius, in design units
  const GLOW_DOT = 1.70;              // extra dot alpha at the centre of it
  const GLOW_LNK = 2.30;              // extra link alpha
  const HOT      = [1.000, 0.965, 0.898];   // what both drift towards

  // ...and leaves ripples behind as it moves
  const HOV_MAX  = 4;                 // rings alive at once
  const HOV_V    = 0.62;              // expansion, design units per second
  const HOV_W    = 0.038;             // ring thickness
  const HOV_LIFE = 0.95;              // seconds
  const HOV_GAP  = 0.26;              // shortest interval between rings
  const HOV_MELT = 1.20;              // how hard a ring melts what it crosses

  const INK      = [0.953, 0.925, 1.000];   // glyphs
  const DOT      = [0.765, 0.651, 1.000];   // dots
  const LINK     = [0.596, 0.435, 0.937];   // mesh
  const BG       = [0.094, 0.039, 0.188];   // deep purple ground
  const DOT_A    = 0.62;
  const LINK_A   = 0.30;

  const TAU = Math.PI * 2;

  const cvs   = document.getElementById('c');
  const tag   = document.getElementById('tag');
  const gl    = cvs.getContext('webgl2', { antialias:false, alpha:false, powerPreference:'high-performance' });
  if (!gl) { document.getElementById('nope').style.display = 'grid'; return; }

  /* ================= glyph atlas ================= */
  const ATLAS_COLS = 8;
  const ATLAS_ROWS = Math.ceil(ALPHABET.length / ATLAS_COLS);
  const CELL       = 96;
  const ATLAS_FS   = CELL / 1.4;      // so cap = 0.498 * CELL, plenty of margin
  const charIndex  = new Map();
  for (let i = 0; i < ALPHABET.length; i++) charIndex.set(ALPHABET[i], i);

  function buildAtlas() {
    const cv = document.createElement('canvas');
    cv.width  = ATLAS_COLS * CELL;
    cv.height = ATLAS_ROWS * CELL;
    const x = cv.getContext('2d');
    x.clearRect(0, 0, cv.width, cv.height);
    x.fillStyle = '#fff';
    x.textAlign = 'center';
    x.textBaseline = 'alphabetic';
    x.font = \`\${ATLAS_FS}px 'JB Mono', 'JetBrains Mono', monospace\`;
    const capPx = ATLAS_FS * CAP;
    for (let i = 0; i < ALPHABET.length; i++) {
      const ch = ALPHABET[i];
      if (ch === ' ') continue;
      const cx = (i % ATLAS_COLS) * CELL + CELL / 2;
      const cy = Math.floor(i / ATLAS_COLS) * CELL + CELL / 2;
      x.fillText(ch, cx, cy + capPx / 2);   // cap box centred in the cell
    }
    return cv;
  }

  /* ================= shared shader half ================= */
  // Both passes need to know, for an arbitrary (ring, slot), where its dot
  // sits and how melted it is — the mesh pass asks about its neighbour too.
  const PRELUDE = \`#version 300 es
  precision highp float;
  in vec4 aA;            // ring index | slot index | glyph index | spare
  uniform vec2  uRes;
  uniform vec2  uCentre;
  uniform float uS;      // the design unit, in device px
  uniform float uTime;
  uniform vec2  uCursor;
  uniform float uCursorOn;
  uniform float uCharge;
  uniform int   uRingMax;
  uniform float uR0, uDR;    // ring 1 radius, ring pitch — the lattice to snap to
  uniform vec4  uHov[\${HOV_MAX}];   // pointer ripples: x | y | front radius | strength
  uniform sampler2D uRing;   // row 0: rot | wave melt | base melt | radius
                             // row 1: angular slot pitch

  float hash31(vec3 p){
    p = fract(p * 0.1031);
    p += dot(p, p.yzx + 33.33);
    return fract((p.x + p.y) * p.z);
  }
  float hash21(vec2 p){
    vec3 q = fract(vec3(p.x, p.y, p.x) * vec3(0.1031, 0.1030, 0.0973));
    q += dot(q, q.yzx + 33.33);
    return fract((q.x + q.y) * q.z);
  }
  // value noise that wraps seamlessly every N cells on x (the angular axis)
  float vnoise(vec3 x, float N){
    vec3 i = floor(x), f = fract(x);
    f = f * f * (3.0 - 2.0 * f);
    float x0 = mod(i.x, N), x1 = mod(i.x + 1.0, N);
    vec3 a0 = vec3(x0, i.y, i.z), a1 = vec3(x1, i.y, i.z);
    float n00 = mix(hash31(a0), hash31(a1), f.x);
    float n10 = mix(hash31(a0 + vec3(0,1,0)), hash31(a1 + vec3(0,1,0)), f.x);
    float n01 = mix(hash31(a0 + vec3(0,0,1)), hash31(a1 + vec3(0,0,1)), f.x);
    float n11 = mix(hash31(a0 + vec3(0,1,1)), hash31(a1 + vec3(0,1,1)), f.x);
    return mix(mix(n00, n10, f.y), mix(n01, n11, f.y), f.z);
  }

  float ringRot(int ri){ return texelFetch(uRing, ivec2(ri, 0), 0).x; }
  float ringStep(int ri){ return texelFetch(uRing, ivec2(ri, 1), 0).x; }

  // how much the pointer lifts a point out of the ground
  float glow(vec2 p){
    float t = clamp(distance(p, uCursor) / (uS * \${GLOW_R}), 0.0, 1.0);
    return uCursorOn * (1.0 - t * t);      // broad in the middle, soft at the rim
  }

  // a slow field that decides where the mesh coarsens into bigger polygons —
  // neighbouring nodes read almost the same value, so the change is regional
  float coarseField(float u, float rv){
    const float N = \${N_CELLS}.0;
    return vnoise(vec3(u * N + 3.3, rv * 0.45 + 21.0, uTime * 0.045), N);
  }

  // how brightly the pointer's ripples are washing over a point
  float ripple(vec2 p){
    float h = 0.0;
    for (int i = 0; i < \${HOV_MAX}; i++){
      if (uHov[i].w <= 0.0) continue;
      float x = (distance(p, uHov[i].xy) - uHov[i].z) / (uS * \${HOV_W});
      h += uHov[i].w * exp(-x * x);
    }
    return min(h, 1.0);
  }

  // 0 = a glyph, 1 = melted all the way down to its dot
  float slotState(int ri, float slot, out vec2 dotPos, out vec2 glyphPos, out vec2 dir, out float fs, out float hot){
    vec4  rd = texelFetch(uRing, ivec2(ri, 0), 0);
    float st = texelFetch(uRing, ivec2(ri, 1), 0).x;
    float R  = rd.w;
    float adv = uS * \${K_ADV_A.toFixed(8)} + R * \${K_ADV_B.toFixed(8)};
    fs = adv / \${EM_ADV};

    float a = rd.x - slot * st;
    dir = vec2(cos(a), sin(a));
    glyphPos = uCentre + (R - 0.0768 * fs) * dir;   // glyph centre sits inside the dot ring

    // a node is nudged off its slot so the mesh triangulates unevenly
    vec2  tang = vec2(-dir.y, dir.x);
    float jr = hash21(vec2(float(ri) * 1.7 + 0.3, slot * 0.9 + 4.1)) - 0.5;
    float jt = hash21(vec2(slot * 1.3 + 2.7, float(ri) * 2.1 + 9.4)) - 0.5;
    dotPos = uCentre + (R + jr * uDR * \${2 * JIT_R}) * dir + tang * (jt * adv * \${2 * JIT_T});

    // a slow, smooth field decides which stretches of the phrase stay legible
    float u  = a * 0.1591549 + 0.5;
    float rv = R / (uS * \${N_RADIAL});
    const float N = \${N_CELLS}.0;
    float f = mix(vnoise(vec3(u * N,       rv,             uTime * \${N_DRIFT}),       N),
                  vnoise(vec3(u * N * 2.0, rv * 2.3 + 5.1, uTime * \${N_DRIFT * 1.7}), N * 2.0),
                  \${N_MIX});
    float m = 0.5 + (f - 0.5 + rd.z) * \${N_AMP.toFixed(2)} + uCharge;
    m = max(m, rd.y);
    float cm = \${WAVE_AMP} * (1.0 - smoothstep(uS * \${CUR_IN.toFixed(8)}, uS * \${CUR_OUT.toFixed(8)}, distance(glyphPos, uCursor)));
    m = max(m, cm * uCursorOn);

    hot = ripple(glyphPos);
    m = max(m, hot * \${HOV_MELT});

    // a glyph does not fade — it shrinks into its dot
    return clamp((m - hash21(vec2(float(ri), slot))) / \${MELT_W} + 0.5, 0.0, 1.0);
  }

  vec2 quadCorner(int vid){ return vec2(float(vid & 1) - 0.5, float(vid >> 1) - 0.5); }
  vec4 toClip(vec2 p){ vec2 n = (p / uRes) * 2.0 - 1.0; return vec4(n.x, -n.y, 0.0, 1.0); }
  \`;

  /* ================= type pass ================= */
  const VS_TYPE = PRELUDE + \`
  uniform float uCols, uRows;
  out vec2  vUV0;
  out vec2  vUVs;
  out vec2  vLocal;
  out float vMelt;
  out float vQuad;
  out float vDotR;
  out float vGlow;

  void main(){
    int   ri   = int(aA.x + 0.5);
    float slot = aA.y;
    vec2 dotPos, gpos, dir; float fs, hot;
    vMelt = slotState(ri, slot, dotPos, gpos, dir, fs, hot);
    vGlow = min(1.0, glow(dotPos) + hot * 1.25);

    float quad = fs * 1.36;   // atlas cell -> cap height on screen
    vec2  rt   = vec2(dir.y, -dir.x);
    vec2  up   = -dir;
    vec2  seat = mix(gpos, dotPos, vMelt * vMelt);   // a melting glyph settles onto its node

    vec2 corner = quadCorner(gl_VertexID);
    vLocal = corner;
    vec2 p = seat + rt * (corner.x * quad) - up * (corner.y * quad);

    float col = mod(aA.z, uCols), row = floor(aA.z / uCols);
    vUV0 = vec2(col / uCols, row / uRows);
    vUVs = vec2(1.0 / uCols, 1.0 / uRows);

    vQuad = quad;
    vDotR = max(0.7, \${DOT_R} * fs);
    gl_Position = toClip(p);
  }\`;

  const FS_TYPE = \`#version 300 es
  precision highp float;
  in vec2  vUV0;
  in vec2  vUVs;
  in vec2  vLocal;
  in float vMelt;
  in float vQuad;
  in float vDotR;
  in float vGlow;
  uniform sampler2D uAtlas;
  uniform float uOpacity;
  uniform float uDotA;
  uniform float uGlowDot;
  uniform vec3  uInk;
  uniform vec3  uDot;
  uniform vec3  uHot;
  out vec4 o;
  void main(){
    float gs = max(1.0 - vMelt, 0.015);
    vec2  g2 = vLocal / gs;
    float g  = texture(uAtlas, vUV0 + (g2 + 0.5) * vUVs).a;
    g = clamp(g * (1.0 + 3.2 * vMelt), 0.0, 1.0);
    g *= step(max(abs(g2.x), abs(g2.y)), 0.5);

    // the dot it collapses into sits on the ring itself
    vec2 q = vLocal * vQuad;
    float d = uDotA * (1.0 + uGlowDot * vGlow)
            * (1.0 - smoothstep(vDotR - 0.5, vDotR + 0.5, length(q))) * vMelt * vMelt;
    d = min(d, 1.0);

    float a = g + d * (1.0 - g);
    if (a <= 0.003) discard;
    vec3 c = (uInk * g + mix(uDot, uHot, vGlow * 0.7) * d * (1.0 - g)) / a;
    a *= uOpacity;
    o = vec4(c * a, a);
  }\`;

  /* ================= mesh pass ================= */
  const VS_LINK = PRELUDE + \`
  uniform float uLineW;
  out float vAlpha;
  out float vOff;
  out float vHalf;
  out float vGlow;

  void main(){
    int   ri   = int(aA.x + 0.5);
    float slot = aA.y;
    int   edge = int(aA.w + 0.5);

    vec2 d0, g0, dir0; float fs0, hot0;
    float m0 = slotState(ri, slot, d0, g0, dir0, fs0, hot0);

    // regions of the field carry wider polygons than others
    vec2  rel = d0 - uCentre;
    float u   = atan(rel.y, rel.x) * 0.1591549 + 0.5;
    float cf   = coarseField(u, length(rel) / (uS * \${N_RADIAL}));
    float pick = hash21(vec2(float(ri) * 4.3 + 1.1, slot * 0.7 + 12.9));
    float span = (cf + pick * 0.45) > \${COARSE} ? 2.0 : 1.0;

    // one edge along this ring, two reaching outward: a triangulation
    int   rp;
    float slotP;
    if (edge == 0) {
      rp = ri;
      slotP = slot + span;
    } else {
      rp = ri + int(span);
      float aMe = ringRot(ri) - slot * ringStep(ri);
      slotP = floor((ringRot(rp) - aMe) / ringStep(rp) + 0.5) + (edge == 2 ? span : 0.0);
    }

    float keep = hash21(vec2(slot * 1.7 + float(edge) * 37.1, float(ri) * 2.9 + 5.3));
    bool  ok   = rp <= uRingMax && keep < \${LINK_P};

    vec2 d1, g1, dir1; float fs1, hot1;
    float m1 = ok ? slotState(rp, slotP, d1, g1, dir1, fs1, hot1) : 0.0;

    float a = ok ? m0 * m1 : 0.0;
    vGlow  = min(1.0, glow(mix(d0, d1, 0.5)) + max(hot0, hot1) * 1.25);
    a     *= 1.0 + \${GLOW_LNK} * vGlow;
    vHalf  = uLineW;
    vAlpha = a;

    float L = ok ? distance(d0, d1) : 0.0;
    vec2 corner = quadCorner(gl_VertexID);
    if (a <= 0.004 || L < 0.001) { gl_Position = vec4(2.0, 2.0, 2.0, 1.0); vOff = 0.0; return; }

    vec2 uv = (d1 - d0) / L;
    vec2 n  = vec2(-uv.y, uv.x);
    float hw = vHalf + 1.0;                             // room for the soft edge
    vOff = corner.y * 2.0 * hw;
    gl_Position = toClip(mix(d0, d1, corner.x + 0.5) + n * vOff);
  }\`;

  const FS_LINK = \`#version 300 es
  precision highp float;
  in float vAlpha;
  in float vOff;
  in float vHalf;
  in float vGlow;
  uniform float uOpacity;
  uniform float uLinkA;
  uniform vec3  uLink;
  uniform vec3  uHot;
  out vec4 o;
  void main(){
    float e = 1.0 - smoothstep(vHalf - 0.6, vHalf + 0.6, abs(vOff));
    float a = min(vAlpha * e * uLinkA, 1.0) * uOpacity;
    if (a <= 0.003) discard;
    o = vec4(mix(uLink, uHot, vGlow * 0.65) * a, a);
  }\`;

  /* ================= programs ================= */
  function compile(src, type) {
    const s = gl.createShader(type);
    gl.shaderSource(s, src); gl.compileShader(s);
    if (!gl.getShaderParameter(s, gl.COMPILE_STATUS)) throw new Error(gl.getShaderInfoLog(s) + '\\n' + src);
    return s;
  }
  function link(vs, fs) {
    const p = gl.createProgram();
    gl.attachShader(p, compile(vs, gl.VERTEX_SHADER));
    gl.attachShader(p, compile(fs, gl.FRAGMENT_SHADER));
    gl.bindAttribLocation(p, 0, 'aA');
    gl.linkProgram(p);
    if (!gl.getProgramParameter(p, gl.LINK_STATUS)) throw new Error(gl.getProgramInfoLog(p));
    return p;
  }
  const SHARED = ['uRes','uCentre','uS','uTime','uCursor','uCursorOn','uCharge','uRingMax','uR0','uDR','uHov','uRing','uOpacity'];
  function locs(p, extra) {
    const u = {};
    for (const n of SHARED.concat(extra)) u[n] = gl.getUniformLocation(p, n);
    return u;
  }
  const progType = link(VS_TYPE, FS_TYPE);
  const progLink = link(VS_LINK, FS_LINK);
  const uType = locs(progType, ['uCols','uRows','uAtlas','uDotA','uGlowDot','uInk','uDot','uHot']);
  const uLink = locs(progLink, ['uLinkA','uLink','uHot','uLineW']);

  /* ================= gpu resources ================= */
  const vao = gl.createVertexArray();
  gl.bindVertexArray(vao);
  const bufA = gl.createBuffer();
  gl.bindBuffer(gl.ARRAY_BUFFER, bufA);
  gl.enableVertexAttribArray(0);
  gl.vertexAttribPointer(0, 4, gl.FLOAT, false, 0, 0);
  gl.vertexAttribDivisor(0, 1);

  const MAXRING = 128;
  const ringTex = gl.createTexture();
  gl.activeTexture(gl.TEXTURE1);
  gl.bindTexture(gl.TEXTURE_2D, ringTex);
  gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA32F, MAXRING, 2, 0, gl.RGBA, gl.FLOAT, null);
  for (const [k, v] of [[gl.TEXTURE_MIN_FILTER, gl.NEAREST], [gl.TEXTURE_MAG_FILTER, gl.NEAREST],
                        [gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE], [gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE]])
    gl.texParameteri(gl.TEXTURE_2D, k, v);
  const ringData = new Float32Array(MAXRING * 2 * 4);

  const atlasTex = gl.createTexture();
  function uploadAtlas() {
    gl.activeTexture(gl.TEXTURE0);
    gl.bindTexture(gl.TEXTURE_2D, atlasTex);
    gl.pixelStorei(gl.UNPACK_PREMULTIPLY_ALPHA_WEBGL, false);
    gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, gl.RGBA, gl.UNSIGNED_BYTE, buildAtlas());
    gl.generateMipmap(gl.TEXTURE_2D);
    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER, gl.LINEAR_MIPMAP_LINEAR);
    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MAG_FILTER, gl.LINEAR);
    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);
    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE);
  }

  gl.disable(gl.DEPTH_TEST);
  gl.enable(gl.BLEND);
  gl.blendFunc(gl.ONE, gl.ONE_MINUS_SRC_ALPHA);
  gl.clearColor(BG[0], BG[1], BG[2], 1);

  /* ================= scene state ================= */
  let W = 0, H = 0, S = 0, DPR = 1;
  let rings = [];        // {R, K, step, rot, vel, kick}
  let count = 0;
  const waves = [];      // {t0, amp, reveal, front}

  function mulberry(a) {
    return function () {
      a |= 0; a = a + 0x6D2B79F5 | 0;
      let t = Math.imul(a ^ a >>> 15, 1 | a);
      t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
      return ((t ^ t >>> 14) >>> 0) / 4294967296;
    };
  }

  function build() {
    DPR = Math.min(window.devicePixelRatio || 1, 2);
    W = Math.round(innerWidth  * DPR);
    H = Math.round(innerHeight * DPR);
    cvs.width = W; cvs.height = H;
    // one unit of the design: for a square viewport this is its side, and for
    // any other shape it keeps the same number of rings between centre and corner
    S = Math.hypot(W, H) * 0.70711;

    const dR    = K_DR * S;
    const R0    = K_R0 * S;
    const reach = Math.hypot(W, H) * 0.5 + dR * 2;

    rings = [];
    for (let i = 1; ; i++) {
      const R = R0 + i * dR;
      if (R > reach || rings.length >= MAXRING - 1) break;
      const adv = K_ADV_A * S + K_ADV_B * R;
      const K = Math.max(3, Math.round(TAU * R / adv));
      rings.push({ R, K, step: TAU / K, rot: 0, vel: 0, i, kick: 0 });
    }

    const rk = mulberry(0x5eed01);
    for (const r of rings) r.kick = (r.i % 2 ? -1 : 1) * (0.62 + 0.76 * rk());

    count = rings.reduce((s, r) => s + r.K, 0);
    // block 0 is the slot list the type pass draws; the mesh pass walks all
    // three blocks so every node gets its own three edges
    const A = new Float32Array(count * EDGES * 4);
    let o = 0, ch = 0;
    for (let ri = 0; ri < rings.length; ri++) {
      for (let k = 0; k < rings[ri].K; k++) {
        const c = PHRASE[ch % PHRASE.length]; ch++;
        for (let e = 0; e < EDGES; e++) {
          const j = (e * count + o) * 4;
          A[j + 0] = ri;
          A[j + 1] = k;                   // text advances clockwise on screen
          A[j + 2] = charIndex.get(c) ?? 0;
          A[j + 3] = e;
        }
        o++;
      }
    }
    gl.bindBuffer(gl.ARRAY_BUFFER, bufA); gl.bufferData(gl.ARRAY_BUFFER, A, gl.STATIC_DRAW);

    tag.style.fontSize = Math.min(16, Math.max(7, 0.00833 * (S / DPR))).toFixed(2) + 'px';
    gl.viewport(0, 0, W, H);

    for (const [p, u] of [[progType, uType], [progLink, uLink]]) {
      gl.useProgram(p);
      gl.uniform2f(u.uRes, W, H);
      gl.uniform2f(u.uCentre, W / 2, H / 2);
      gl.uniform1f(u.uS, S);
      gl.uniform1i(u.uRingMax, rings.length - 1);
      gl.uniform1f(u.uR0, R0);
      gl.uniform1f(u.uDR, dR);
      gl.uniform1i(u.uRing, 1);
    }
    gl.useProgram(progType);
    gl.uniform1f(uType.uCols, ATLAS_COLS);
    gl.uniform1f(uType.uRows, ATLAS_ROWS);
    gl.uniform1i(uType.uAtlas, 0);
    gl.uniform1f(uType.uDotA, DOT_A);
    gl.uniform1f(uType.uGlowDot, GLOW_DOT);
    gl.uniform3fv(uType.uInk, INK);
    gl.uniform3fv(uType.uDot, DOT);
    gl.uniform3fv(uType.uHot, HOT);
    gl.useProgram(progLink);
    const crisp = DPR >= 1.5;
    gl.uniform1f(uLink.uLineW, crisp ? LINK_W : 0.42);
    gl.uniform1f(uLink.uLinkA, LINK_A * (crisp ? 1 : 0.74));
    gl.uniform3fv(uLink.uLink, LINK);
    gl.uniform3fv(uLink.uHot, HOT);
  }

  /* ================= interaction ================= */
  const cursor = { x: -9999, y: -9999, on: 0, want: 0 };
  const hovers = [];                   // rings the pointer has shed
  const hovData = new Float32Array(HOV_MAX * 4);
  let lastHover = -9;
  let holding = false, holdStart = 0, charge = 0, chargeBump = 0;
  let clock = 0;                       // animation time, immune to tab-away jumps
  let last = performance.now() / 1000;

  function setTag(text) { if (tag.textContent !== text) tag.textContent = text; }

  function moveTag(cx, cy) {
    tag.style.transform = \`translate3d(\${cx - 1}px,\${cy + 11}px,0)\`;
  }

  function shedRipple(strength) {
    if (hovers.length >= HOV_MAX) hovers.shift();
    hovers.push({ x: cursor.x, y: cursor.y, t0: clock, s: strength });
    lastHover = clock;
  }

  function pointer(e) {
    const nx = e.clientX * DPR, ny = e.clientY * DPR;
    const moved = Math.hypot(nx - cursor.x, ny - cursor.y);
    cursor.x = nx; cursor.y = ny;
    if (!cursor.want || (moved > S * 0.012 && clock - lastHover > HOV_GAP)) shedRipple(1);
    cursor.want = 1;
    moveTag(e.clientX, e.clientY);
    // the field keeps reacting under the chrome, but the pill gets out of its way
    const onChrome = e.target && e.target.closest && e.target.closest('a,button');
    tag.classList.toggle('on', !onChrome);
  }
  addEventListener('pointermove', pointer, { passive: true });
  addEventListener('pointerdown', (e) => {
    pointer(e);
    shedRipple(1.35);
    holding = true;
    holdStart = clock;
  });
  function release(e) {
    if (!holding) return;
    holding = false;
    if (charge > 0.06) waves.push({ t0: clock, amp: 0.45 + 0.55 * charge, reveal: false, front: 0 });
    if (e && e.pointerType === 'touch') { cursor.want = 0; tag.classList.remove('on'); }
  }
  addEventListener('pointerup', release);
  addEventListener('pointercancel', release);
  addEventListener('blur', release);
  addEventListener('pointerleave', () => { cursor.want = 0; tag.classList.remove('on'); });
  addEventListener('contextmenu', (e) => e.preventDefault());

  /* ================= frame ================= */
  function frame() {
    const now = performance.now() / 1000;
    const dt = Math.min(now - last, 1 / 20); last = now;
    clock += dt;
    const t = clock;

    // charge -----------------------------------------------------------
    if (holding) {
      const held = clock - holdStart;
      charge = Math.min(1, held / CHARGE_T);
      chargeBump = charge * Math.exp(-Math.max(0, held - CHARGE_T) / 0.9);
      setTag(charge >= 1 ? 'RELEASE' : 'KEEP HOLDING');
    } else {
      charge = Math.max(0, charge - dt * 4);
      chargeBump += (0 - chargeBump) * Math.min(1, dt * 3.2);
      setTag('CLICK & HOLD');
    }

    cursor.on += (cursor.want - cursor.on) * Math.min(1, dt * 12);

    // pointer ripples -----------------------------------------------------
    hovData.fill(0);
    for (let i = hovers.length - 1; i >= 0; i--) {
      const age = clock - hovers[i].t0;
      if (age > HOV_LIFE) { hovers.splice(i, 1); continue; }
      const k = 1 - age / HOV_LIFE;
      const j = i * 4;
      hovData[j    ] = hovers[i].x;
      hovData[j + 1] = hovers[i].y;
      hovData[j + 2] = HOV_V * S * age;
      hovData[j + 3] = hovers[i].s * Math.pow(k, 0.9);
    }

    // waves ------------------------------------------------------------
    const v = WAVE_V * S, halfW = WAVE_W * S;
    for (let i = waves.length - 1; i >= 0; i--) {
      const w = waves[i];
      const prev = v * (t - w.t0 - dt);
      const cur  = v * (t - w.t0);
      w.front = cur;
      for (const r of rings) {
        if (prev < r.R && cur >= r.R) r.vel += SPIN_KICK * w.amp * r.kick;
      }
      if (cur > Math.hypot(W, H) * 0.5 + halfW * 2.5) waves.splice(i, 1);
    }

    // per-ring upload ---------------------------------------------------
    const decay = Math.exp(-dt / SPIN_TAU);
    for (let i = 0; i < rings.length; i++) {
      const r = rings[i];
      r.rot += r.vel * dt;
      r.vel *= decay;

      let melt = 0;
      for (const w of waves) {
        if (w.reveal) {
          // the opening wave is a clean edge: everything it has passed is written
          const e = S * 0.05;
          const u = Math.min(1, Math.max(0, (r.R - w.front + e) / (2 * e)));
          melt = Math.max(melt, WAVE_AMP * u * u * (3 - 2 * u));
        } else {
          const x = (r.R - w.front) / halfW;
          melt = Math.max(melt, WAVE_AMP * w.amp * Math.exp(-x * x));
        }
      }
      ringData[i * 4 + 0] = r.rot;
      ringData[i * 4 + 1] = melt;
      ringData[i * 4 + 2] = R_BASE - R_SLOPE * Math.min(1, r.R / (S * 0.72));  // radial bias
      ringData[i * 4 + 3] = r.R;
      ringData[(MAXRING + i) * 4 + 0] = r.step;
    }
    gl.activeTexture(gl.TEXTURE1);
    gl.bindTexture(gl.TEXTURE_2D, ringTex);
    gl.texSubImage2D(gl.TEXTURE_2D, 0, 0, 0, MAXRING, 2, gl.RGBA, gl.FLOAT, ringData);

    // draw --------------------------------------------------------------
    const op = Math.min(1, t / 0.55);
    gl.clear(gl.COLOR_BUFFER_BIT);
    gl.bindVertexArray(vao);
    for (const [p, u, n] of [[progLink, uLink, count * EDGES], [progType, uType, count]]) {
      gl.useProgram(p);
      gl.uniform1f(u.uTime, t);
      gl.uniform2f(u.uCursor, cursor.x, cursor.y);
      gl.uniform1f(u.uCursorOn, cursor.on);
      gl.uniform1f(u.uCharge, chargeBump * CHARGE_M);
      gl.uniform1f(u.uOpacity, op);
      gl.uniform4fv(u.uHov, hovData);
      gl.drawArraysInstanced(gl.TRIANGLE_STRIP, 0, 4, n);
    }

    requestAnimationFrame(frame);
  }

  /* ================= boot ================= */
  let resizeTimer = 0;
  addEventListener('resize', () => {
    clearTimeout(resizeTimer);
    resizeTimer = setTimeout(build, 120);
  });

  document.fonts.load("16px 'JB Mono'").catch(() => {}).then(() => {
    uploadAtlas();
    build();
    clock = 0;
    last = performance.now() / 1000;
    waves.push({ t0: INTRO_T, amp: 1, reveal: true, front: 0 });
    requestAnimationFrame(frame);
  });
})();
<\/script>
</body>
</html>
`,v=`<script>
document.addEventListener("click", function (event) {
  const anchor = event.target && event.target.closest ? event.target.closest("a[href]") : null;
  if (anchor) event.preventDefault();
});
<\/script>`,m=f.replace("</body>",`${v}</body>`);function x({className:r="",style:c}){const i=n.useRef(null),[d,u]=n.useState(()=>typeof document>"u"||!document.hidden),[g,p]=n.useState(!0),[e,a]=n.useState(!1);n.useEffect(()=>{const t=i.current;if(!t||typeof IntersectionObserver>"u")return;const s=new IntersectionObserver(([h])=>{p(h?.isIntersecting??!0)},{rootMargin:"80px"});return s.observe(t),()=>s.disconnect()},[]),n.useEffect(()=>{if(typeof document>"u")return;const t=()=>u(!document.hidden);return document.addEventListener("visibilitychange",t),()=>document.removeEventListener("visibilitychange",t)},[]);const o=g&&d;return n.useEffect(()=>{a(!1)},[o]),l.jsx("div",{ref:i,className:`web3dkit-background web3dkit-rings-hero${r?` ${r}`:""}`,role:"group","aria-label":"Interactive Web3DKit concentric typography rings hero","data-state":o?e?"ready":"loading":"paused",style:{background:"#180a30",pointerEvents:"auto",...c},children:o?l.jsx("iframe",{title:"Web3DKit — Three.js components",srcDoc:m,sandbox:"allow-scripts",loading:"eager",onLoad:()=>a(!0),style:{position:"absolute",inset:0,display:"block",width:"100%",height:"100%",border:0,background:"#180a30",opacity:e?1:0,pointerEvents:e?"auto":"none",transition:"opacity 240ms ease-out"}}):null})}export{x as Web3DKitRingsHero};
